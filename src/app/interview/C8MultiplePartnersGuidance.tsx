"use client";

import type { C8AnalSexResponse } from "./c8-multiple-partners-guidance";

export { parseC8AnalSexResponse } from "./c8-multiple-partners-guidance";

/** Center panel no longer shows C8 guidance — see Guidance panel on the right. */
export function C8MultiplePartnersGuidance(_props: {
  analSexResponse: C8AnalSexResponse | null;
}) {
  return null;
}
